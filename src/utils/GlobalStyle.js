import {StyleSheet} from 'react-native';

export default StyleSheet.create({
  bigText: {
    flex: 1,
    fontSize: 30,
    fontWeight: 'bold',
    alignSelf: 'center',
    textAlignVertical: 'center',
  },
});
